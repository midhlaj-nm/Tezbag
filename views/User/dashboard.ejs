<%- include("../../views/Partials/user/header-ho") %>
<style>
    #toast-container>.toast {
        opacity: 1;
    }

    .toast-success {
        background-color: #4CAF50;
    }

    .toast-error {
        background-color: #e92e20;
    }
</style>
<hr style="margin: 0rem 0rem 1rem 0rem;">
<link rel="stylesheet" href="/User-css/dashboard.css">
<script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>

<!-- Toastr CSS & JS -->
<link href="https://cdnjs.cloudflare.com/ajax/libs/toastr.js/latest/toastr.min.css" rel="stylesheet"/>
<script src="https://cdnjs.cloudflare.com/ajax/libs/toastr.js/latest/toastr.min.js"></script>

<!-- Bootstrap CSS -->
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">

<!-- Font&Icon -->
<link href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined" rel="stylesheet">
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">
<link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600;700&display=swap" rel="stylesheet">

  <!-- breadcrumb -->
  <nav aria-label="breadcrumb">
    <ol class="breadcrumb">
      <li class="breadcrumb-item"><a href="/">Home</a></li>
      <% breadcrumbs.forEach((crumb, index) => { %>
        <li class="breadcrumb-item <%= index === breadcrumbs.length - 1 ? 'active' : '' %>">
          <% if (index !== breadcrumbs.length - 1) { %>
            <a href="<%= crumb.url %>"><%= crumb.name %></a>
          <% } else { %>
            <%= crumb.name %>
          <% } %>
        </li>
      <% }) %>
    </ol>
  </nav>

<div class="page-wrapper">
  <!-- =======================Left area================= -->
  <div class="navigation-panel">
    <div class="nav-header">Navigation</div>
    
    <div class="nav-item active" onclick="window.location.href='/account'">
      <span class="material-symbols-outlined">Dashboard</span>Dashboard
    </div>
    <div class="nav-item" onclick="window.location.href='/order-history'">
      <i class="fa-solid fa-rotate"></i> Order History
    </div>
    <div class="nav-item" onclick="window.location.href='/wishlist'">
      <span class="material-symbols-outlined">favorite</span>Wishlist
    </div>
    <div class="nav-item" onclick="window.location.href='/cart'">
      <span class="material-symbols-outlined">shopping_cart</span>Shopping Cart
    </div>
    <div class="nav-item" onclick="window.location.href='/account-settings'">
      <span class="material-symbols-outlined">settings</span>Settings
    </div>
    <div class="nav-item" onclick="window.location.href='/wallet'">
      <span class="material-symbols-outlined">wallet</span>Wallet
    </div>
    <div class="nav-item" onclick="window.location.href='/logout'">
      <span class="material-symbols-outlined">logout</span>Log-out
    </div>
  </div>

  <!-- =================== right container ======================= -->
  <div class="rgh-container">
    <img src="/profile pic.jpg" alt="TezBag Profile Pic">
    <h4><%= userName %></h4>
    <p class="user-role">Customer</p>
    <a href="/account-settings" class="custom-link">
      <h5>Edit Profile</h5>
    </a>      
  </div>  
  <div class="rgh-container2">
    <p class="add">ADDRESS</p>
    <h4><%= userName %></h4>
    <p class="mail"><%= userEmail%></p>
    <% if (address && address !== 'Not provided') { %>
        <p class="add">
          <%= address.streetAddress || '' %>
          <%= address.city ? ', ' + address.city : '' %>
          <%= address.state ? ', ' + address.state : '' %>
          <%= address.pinCode ? ', ' + address.pinCode : '' %>
        </p>
        <p class="num"><%= address.phone%></p>
      <% } else { %>
        <p style="color: #7b7b7b; margin: 1rem 0;">Add Address to see magic!</p>
      <% } %>
    <a href="/account-settings" class="custom-link-add">
      <h5>Add/Edit Address</h5>
    </a>
  </div>
<div class="rgh-container3">
  <div class="recent-order-flex">
    <div>
      <h4 style="margin-bottom: 0; padding-top: .5rem;">Give 20₹, Earn 10₹</h4>
    </div> 
    <div>
      <button class="refer-btn" id="invite-btn">
        <svg xmlns="http://www.w3.org/2000/svg" height="20px" viewBox="0 -960 960 960" width="20px" fill="#FFFFFF">
          <path d="M432-288H288q-79.68 0-135.84-56.23Q96-400.45 96-480.23 96-560 152.16-616q56.16-56 135.84-56h144v72H288q-50 0-85 35t-35 85q0 50 35 85t85 35h144v72Zm-96-156v-72h288v72H336Zm192 156v-72h144q50 0 85-35t35-85q0-50-35-85t-85-35H528v-72h144q79.68 0 135.84 56.23 56.16 56.22 56.16 136Q864-400 807.84-344 751.68-288 672-288H528Z"/>
        </svg>Invite now
      </button>
    </div>
  </div>
</div>

<!-- ======================== logout modal ================================= -->
<div id="logout-modal" class="modal" style="display:none;">
  <div class="modal-content">
    <h3>Are you sure?</h3>
    <div class="modal-buttons">
      <button id="cancel-logout" class="btn-no">NO</button>
      <button id="confirm-logout" class="btn-yes">YES</button>
    </div>
  </div>
</div>

<script>
document.addEventListener('DOMContentLoaded', function() {
  const inviteBtn = document.getElementById('invite-btn');
  const referralUrl = '<%= referralUrl %>'; 

  inviteBtn.addEventListener('click', function() {
    if (referralUrl) {
      if (navigator.share) {
        navigator.share({
          title: 'Join TezBag with my Referral!',
          text: 'Log in using my referral link and get 20₹ credit!',
          url: referralUrl,
        }).then(() => {
          console.log('Linked Shared')
        }).catch(err => {
          navigator.clipboard.writeText(referralUrl).then(() => {
            toastr.success('Referral link copied to clipboard!');
          });
        });
      } else {
        navigator.clipboard.writeText(referralUrl).then(() => {
          toastr.success('Referral link copied to clipboard!');
        }).catch(err => {
          toastr.error('Failed to copy link.');
        });
      }
    }
  });

  const logoutBtn = document.querySelector('.nav-item[onclick*="logout"]') || document.querySelector('.log-out');
  const modal = document.getElementById('logout-modal');
  const cancelBtn = document.getElementById('cancel-logout');
  const confirmBtn = document.getElementById('confirm-logout');
  
  if (logoutBtn) {
    const originalOnClick = logoutBtn.getAttribute('onclick');
    logoutBtn.removeAttribute('onclick');
    
    logoutBtn.addEventListener('click', function(e) {
      e.preventDefault();
      modal.style.display = 'flex';
    });
  }
  
  cancelBtn.addEventListener('click', function() {
    modal.style.display = 'none';
  });
  
  confirmBtn.addEventListener('click', function() {
    window.location.href = '/logout';
  });
  
  window.addEventListener('click', function(e) {
    if (e.target === modal) {
      modal.style.display = 'none';
    }
  });
});
</script>

<%- include("../../views/Partials/user/footer") %>